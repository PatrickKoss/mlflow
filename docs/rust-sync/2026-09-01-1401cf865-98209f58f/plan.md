# Rust upstream sync plan — 2026-09-01

Status: **COMPLETE (2026-09-01)** · From: `1401cf865aa2d26f22d4b36687fc3fc9008d413e` · To: `98209f58fe27c124f0cd28a69e1b3ea80ba0d804`

| Bucket       | Commits |
| ------------ | ------: |
| `server-api` |      62 |
| `ui`         |      44 |
| `client-sdk` |      30 |
| `infra`      |     172 |

When in doubt, the merged upstream Python implementation is the behavioral spec.

Upstream merge: branch `sync/upstream-2026-09-01`, merge commit `33dd378a3`
(only conflict: `tests/store/tracking/sqlalchemy_store/test_sqlalchemy_workspace_store.py`,
both sides added a test at the same spot; kept both). 369 files changed under
`mlflow/server/js`, so the production UI was rebuilt post-merge (Phase 1, see T-S11).

Five `infra`-bucket commits carry server behavior and are pulled into tasks below rather
than skipped: `847fca8031` (fail-closed default flips to on), `e6a3c71309` (dataset `IN`
clause parsing), `c3bfb448d4` (`search_logged_models` quoted values / operator message),
`cd13d3cdc9` (demo seeds a custom view).

Task ordering: T-S1 is coordinator-executed first because the merged Python alembic head
(`b7e2c1a4d9f3`) no longer matches the Rust pin and every other gate refuses a DB at the
new head. T-S3 depends on T-S1 and T-S2 (it adds a job validator to the same dispatch
tables and reads the new `jobs.creator` column). Everything else can run in parallel.

## Tasks

- [x] T-S1 Advance the pinned alembic head to `b7e2c1a4d9f3` (three new upstream migrations)
  - **Upstream refs:** `3128eb4692` perf(tracing): index trace archival candidate ordering (#24976) → migration `1971f2d9a75b` (index `index_trace_info_timestamp_ms_request_id` on `trace_info(timestamp_ms, request_id)`); `ffa0e39ea0` [Custom View] Increase experiment tag value limit to 20000 (#25062) → migration `17e22815139b` (`experiment_tags.value` → Text / MEDIUMTEXT / NVARCHAR(max)); `3e5a36daf9` [4/6] basic-auth: gate job routes on per-job ownership (#25069) → migration `b7e2c1a4d9f3` (`jobs.creator` String(255) nullable)
  - **Rust target:** `rust/crates/mlflow-store/src/db.rs` (`EXPECTED_ALEMBIC_HEAD`), `rust/crates/mlflow-store/tests/connect_verify.rs`, doc/comment references in `rust/tools/make_test_db.py`, `rust/docs/MIGRATION_RUNBOOK.md`, `rust/bench/RESTRUCTURES.md`, `rust/crates/mlflow-store/tests/{stress_trace_writes,workspaces_store}.rs`, `rust/crates/mlflow-store/src/schema/mcp.rs`
  - **AC:** Rust connects to a DB migrated by the merged Python `mlflow db upgrade` (head `b7e2c1a4d9f3`) and refuses `6f8d9c3b2a1e` as OutOfDate with the same message shape as before. Rust never migrates. No Rust behavior change beyond the pin; the three migrations are schema-only (index, wider column, nullable column) and the store's existing queries remain valid. `1971f2d9a75b` needs no Rust change: the archival candidate query already orders by `(timestamp_ms, request_id)`; note the evidence in the DONE entry.
  - **VER:** `cargo test -p mlflow-store --test connect_verify`; `uv run --no-sync python rust/tools/make_test_db.py` reports the new head; `rust/compliance/recorders/test_gateway_runtime_differential.py` parses the new constant (`uv run --no-sync pytest -q rust/compliance/recorders/ -k gateway_runtime`).
  - **DONE 2026-09-01** (coordinator, commit `c1bbe70aa`): `EXPECTED_ALEMBIC_HEAD` = `b7e2c1a4d9f3` in `rust/crates/mlflow-store/src/db.rs`; `rust/crates/mlflow-store/tests/fixtures/tracking.db` migrated in place with `mlflow db upgrade` (keeps the pre-seeded run `3e47c4f1c03444f0bb7c50b4828de835` that `graphql_http.rs` pins; a first attempt regenerated it with `make_test_db.py` and broke seven GraphQL tests) and copied to `rust/crates/mlflow-server/tests/fixtures/tracking.db`. `connect_verify` 8/8 green; recorder `-k gateway_runtime` 2 passed; `cargo test --workspace --no-fail-fast` green except: the `supported-models` byte golden in `gateway_discovery_http.rs:305` (model-catalog refresh, T-S10); `gateway_runtime_http.rs:55` where `rust/tools/gateway_routing_oracle.py` now raises `scripted stream failure` under upstream's new stream-fallback semantics (T-S6 must update the oracle); the promptlab env/version pins (T-S12). `1971f2d9a75b` evidence: `rust/crates/mlflow-server/src/trace_archival.rs:510` already orders candidates by `ti.timestamp_ms, ti.request_id`.

- [x] T-S2 Basic-auth fail-closed net: datasets, issues, gateway config/discovery, list filters, presigned/invoke/demo/metric-twin, native-route fail-closed
  - **Upstream refs:** `298cfa19fa` [1/6] basic-auth: fail-closed authorization net + coverage guard (#25065); `847fca8031` basic-auth: enable fail-closed authorization by default (#25308); `8b1131395f` [2/6] gate evaluation dataset routes on experiment permission (#25066); `8dd281a04a` [3/6] gate issue routes on experiment permission (#25067); `9f7c469233` [5/6] gate gateway guardrail/budget/config/discovery + filter list endpoints (#25070); `78de76035d` redact gateway secrets-config passphrase signal from non-admins (#25298); `64cfe5499e` [6/6] gate presigned/invoke/demo/metric-twin; empty the debt list (#25071); `549a5fdd20` [7/8] fail-closed enforcement for native FastAPI routes (#25101)
  - **Rust target:** `rust/crates/mlflow-server/src/auth_middleware/{path_matchers,validators,after_request,mod}.rs`, `rust/crates/mlflow-auth` (read helpers for dataset experiment ids / issue experiment id), `rust/crates/mlflow-server/src/{datasets,issues,gateway,demo,metric_history}.rs` as needed for request parsing, `rust/crates/mlflow-server/tests/auth_middleware*.rs`
  - **AC:** With basic auth enabled, Rust matches merged Python for every route in the series. (1) `MLFLOW_BASIC_AUTH_FAIL_CLOSED` defaults to **true**; authenticated requests to a route with no authorization decision get 403 `Permission denied`; `false` restores fail-open. This applies to proto routes, Flask-style paths, and FastAPI-native routers (assistant, jobs, gateway runtime, OTLP) alike. (2) Evaluation datasets: get/update/delete require the corresponding experiment permission on **all** of the dataset's experiment ids (`get_dataset_experiment_ids`); create/search require the permission on the `experiment_ids` from the JSON body or query args; `add-to-experiments` requires update on the dataset's experiments and the added ones; unknown `/mlflow/datasets/` paths deny. (3) Issues: get/update derive the experiment from `get_issue(issue_id).experiment_id`; create/search read `experiment_id` from the normalized request JSON and require update/read; a missing experiment id is a denial; unknown `/mlflow/issues/` paths deny; `/mlflow/issues/invoke` and `/mlflow/genai/evaluate` (`INVOKE_ISSUE_DETECTION`, `INVOKE_GENAI_EVALUATE`, POST) require `validate_can_update_experiment`. (4) Gateway: budget policy writes admin-only; Get/List budget policies and ListGatewayBudgetWindows allow any authenticated user; guardrail CRUD admin-only; Add/Remove/UpdateEndpointGuardrailConfig require update on the endpoint; ListEndpointGuardrailConfigs requires read on the endpoint; `GATEWAY_SUPPORTED_PROVIDERS`, `SUPPORTED_MODELS`, `PROVIDER_CONFIG`, `SECRETS_CONFIG` GETs allow any authenticated user; after-request filters `filter_list_gateway_endpoints/model_definitions/secrets` hide rows a non-admin cannot read (`_role_based_read_predicate`); `redact_secrets_config_for_non_admins` strips `using_default_passphrase` for non-admins. (5) `CreatePresignedUploadUrl` requires update on the run; `GET /api/2.0/mlflow/metrics/get-history-bulk-interval` uses the same validator as `get-history-bulk`; `DEMO_GENERATE` POST allows any authenticated user, `DEMO_DELETE` POST is admin-only. (6) Rust keeps no equivalent of `_KNOWN_UNGATED_ROUTE_MARKERS`; instead its dispatch tables must have no fail-open holes: add a Rust unit test that walks every registered route and asserts a validator (or explicit Allow) exists, mirroring Python's coverage guard.
  - **VER:** `cargo test -p mlflow-server --test auth_middleware_http --test auth_middleware_no_default_http` plus path-matcher unit tests for each new route; auth conformance rows for datasets/issues/gateway-config/demo/presigned in `uv run --no-sync python rust/genai-inventory/run_conformance.py --profile required` (auth profile) and `rust/e2e/run.sh` auth phase; corpus replay `uv run --no-sync python rust/compliance/replay.py -k auth` (or the full replay) must stay at 0 non-allowlisted diffs after the default flip.
  - **DONE 2026-09-01** (executor commit rebased as the T-S2 commit on top of `f65813c57`): `MLFLOW_BASIC_AUTH_FAIL_CLOSED` defaults to true (`false`/`0` restore fail-open); dispatch now yields `NoDecision` instead of a silent allow and the middleware denies it; datasets (all-experiments permission via `get_evaluation_dataset_experiment_ids`, create/search from body or query `experiment_ids`, add-to-experiments = dataset update + added experiments), issues (via `get_issue().experiment_id`, create/search from the normalized, possibly double-encoded JSON body, missing id = deny, unknown dataset/issue subpaths deny), gateway (budget policy reads allow, writes admin-only; guardrail CRUD admin-only; endpoint guardrail config update/read on the endpoint; discovery GETs allow; `filter_list_gateway_endpoints/model_definitions/secrets` after-request filters; `using_default_passphrase` stripped for non-admins), presigned upload = update run, `metrics/get-history-bulk-interval` shares the bulk validator (already gated), demo generate allow / delete admin-only, native assistant/jobs/gateway-runtime/MCP/OTLP routes carry explicit decisions, `genai/evaluate` and `issues/invoke` require experiment update. Coverage guard: `path_matchers.rs` walks every registered route and asserts a validator or explicit Allow (no `_KNOWN_UNGATED_ROUTE_MARKERS` equivalent). New `tests/auth_middleware_no_default_http.rs` (fail-closed default, datasets, issues, gateway filtering/redaction, demo, invoke, presigned), 19 corpus cases in `auth.yaml`, 11-row `rust/genai-inventory/basic_auth.yaml` auth suite wired into `run_conformance.py --profile required`, `rust/e2e/tests/auth-ui-smoke.spec.mjs` browser probes. Coordinator VER (rebased onto post-T-S8 head, Python on the main venv): fmt/clippy clean, `--lib auth_middleware` + `auth_middleware_http`, `auth_middleware_no_default_http`, `auth_after_request_no_default_http`, `static_prefix_http` 64 passed / 0 failed, replay `-k auth` 58 cases (auth 56 + auth_artifacts_only 2), 0 diffs, `run_conformance.py --profile required --server-bin rust/target/debug/mlflow-server` green (auth suite 11/11, ledger sqlite + postgres 3+11 each; the first run failed only because the worktree had a release server without a release worker). The auth e2e phase runs inside `bash rust/e2e/run.sh` under T-S11 / phase 4.

- [x] T-S3 Jobs: record `creator` and gate job get/cancel on per-job ownership
  - **Upstream refs:** `3e5a36daf9` [4/6] basic-auth: gate job routes on per-job ownership (#25069); `ce53bdf8a1` [8/8] basic-auth: gate native FastAPI job routes on per-job ownership (#25102); `d8f405d702` jobs: add configurable Huey storage URL for Redis (#22653) (verify N/A)
  - **Rust target:** `rust/crates/mlflow-store/src/schema/jobs.rs`, `rust/crates/mlflow-store/src/store/jobs.rs`, `rust/crates/mlflow-server/src/jobs.rs`, `rust/crates/mlflow-server/src/auth_middleware/{path_matchers,validators}.rs`
  - **AC:** `jobs.creator` (nullable String(255)) is read and written by the Rust store: `submit_job` stores the authenticated username (`request.state.username` / `flask.g.mlflow_authenticated_user`; `NULL` when auth is off) and job entities/JSON carry `creator` exactly where Python's `Job` entity does. `validate_is_job_owner` gates `JOB_GET` (`GET /ajax-api/3.0/mlflow/jobs/<job_id>`) and `JOB_CANCEL` (`PATCH /ajax-api/3.0/mlflow/jobs/cancel/<job_id>`) and the native `/ajax-api/3.0/jobs/{id}` get/cancel routes: admins pass, the creator passes, anyone else gets 403 with Python's message; submit and search require authentication only. Unknown job paths fail closed under T-S2's net. `d8f405d702` (Huey Redis storage, `MLFLOW_SERVER_JOB_HUEY_REDIS_URL`, `MLFLOW_SERVER_JOB_ENABLE_PERIODIC_TASKS`, `MLFLOW_SERVER_JOB_FLUSH_PERIODIC_LOCKS_ON_STARTUP`) is Python-worker plumbing with no Rust analogue (native worker queue is the `jobs` table, plan D20); document N/A with the env-var names in the DONE note.
  - **VER:** `cargo test -p mlflow-store` (jobs store round-trip with creator, sqlite + postgres) and `cargo test -p mlflow-server --test jobs_http --test auth_middleware_http`; new conformance rows: owner get/cancel 200, non-owner 403, admin 200 via `uv run --no-sync python rust/genai-inventory/run_conformance.py --profile required`; job corpus cases in `uv run --no-sync python rust/compliance/replay.py -k jobs`.
  - **DONE 2026-09-01** (executor commit rebased as `a0379045c` on top of `7a414f10f`, ff-merged): `jobs.creator` read/written by `JobStore` (`create_job_with_creator`, `row_to_job`); native `POST /ajax-api/3.0/jobs/` records `AuthContext.username` (NULL without auth) and the invoke / prompt-optimization job submitters pass the same creator; native `POST /ajax-api/3.0/jobs/search` added. `Validator::IsJobOwner` (`validate_is_job_owner`: creator must equal the caller, missing job or NULL creator denies) gates `GET /ajax-api/3.0/mlflow/jobs/<id>`, `PATCH /ajax-api/3.0/mlflow/jobs/cancel/<id>` and the native `/ajax-api/3.0/jobs/{id}` get/cancel; submit/search are authenticated-only; unknown `/mlflow/jobs/*` and `/ajax-api/3.0/jobs*` paths fail closed. Job JSON omits `creator` like Python's FastAPI `Job` response model (`mlflow/server/job_api.py:18`) while the entity carries it. `d8f405d702` is N/A: `MLFLOW_SERVER_JOB_HUEY_REDIS_URL`, `MLFLOW_SERVER_JOB_ENABLE_PERIODIC_TASKS`, `MLFLOW_SERVER_JOB_FLUSH_PERIODIC_LOCKS_ON_STARTUP` configure the Python Huey worker; the Rust native worker queues on the `jobs` table (`job_runner.rs`). Coordinator VER (rebased worktree, Python on the main venv): fmt/clippy (workspace) clean; `cargo test -p mlflow-store` sqlite plus `jobs_store` under postgres and mysql; `jobs_http` + `auth_middleware_http` + `auth_middleware_no_default_http` + `--lib auth_middleware` all passed; replay `-k jobs` (new 7-case `rust/compliance/corpus/jobs.yaml`, Python side booted with job execution enabled) and `-k auth` (11 new setup/assertion cases) exit 0 with no diffs; `run_conformance.py --profile required --server-bin rust/target/debug/mlflow-server` green with the auth suite at 17/17 (six new `jobs` rows: owner get/cancel 200, non-owner 403, admin 200) and the ledger green on sqlite and postgres.

- [x] T-S4 SqlAlchemy store parity: link_prompts_to_trace existence check, log_batch metric ordering/dedup, scorer experiment-id coercion, search parsing fixes
  - **Upstream refs:** `a0524ee818` Validate trace exists in link_prompts_to_trace (#24303); `40c146a235` Fix cross-batch deadlock in concurrent log_batch latest_metrics writes (#24941); `61a5401606` Bound `log_batch` duplicate-metric recovery by batch size (#24683); `998bbc12b0` Fix scorer experiment_id VARCHAR binds on PostgreSQL + psycopg v3 (#25315); `8c6e2b77a4` Fix integer/varchar type mismatch in WorkspaceAwareSqlAlchemyStore experiment_id filters (#25232); `e6a3c71309` Stop `SearchUtils._get_value` from silently dropping dataset IN clause values (#25045); `c3bfb448d4` Fix `search_logged_models` quoted-value parsing and invalid-operator message (#25398); `39dc558e4b` Preserve status in partial `update_webhook` calls (#24842) (verify); `36c7a7f6ec` Offload `log_spans()` from the async event loop (#25185) (verify N/A)
  - **Rust target:** `rust/crates/mlflow-store/src/store/{traces,metrics,metrics_bulk,scorers,experiments,search}.rs`, `rust/crates/mlflow-search/src/domains/{runs,logged_models}.rs` (dataset `IN` on `context`; logged-model operator message), `rust/crates/mlflow-server/src/{traces,scorers,webhooks}.rs`
  - **AC:** (1) `POST /mlflow/traces/{trace_id}/link-prompts` for a missing trace returns RESOURCE_DOES_NOT_EXIST `Trace with ID '{trace_id}' not found.` (Python now checks existence via `_trace_query` before linking; previously it silently succeeded). (2) `log_batch` writes metrics with globally sorted, deduplicated keys so concurrent batches acquire `latest_metrics` locks in a consistent order, and the metrics write path retries once more on a detected deadlock (`_DB_WRITE_MAX_DEADLOCK_RETRIES = 2`); duplicate-metric recovery is bounded to the batch (chunks of `_METRIC_DEDUP_CHUNK_SIZE = 100` exact-identity lookups on `(key, timestamp, step, value, is_nan)`), never a full-run scan. Where Rust already dedups through the 6-column PK `ON CONFLICT DO NOTHING`, document the equivalence and add only the key ordering + retry; the observable contract is identical final `latest_metrics` rows under interleaved batches. (3) Scorer store: `experiment_id` is coerced to an integer before binding on every scorer query (register/list/get/update/delete/online configs); `list_scorers` with a non-integer experiment id raises INVALID_PARAMETER_VALUE `Invalid experiment IDs: experiment IDs must be valid integers.`; the workspace-aware `_filter_experiment_ids` path binds integers on PostgreSQL. (4) Dataset filter `context IN (...)` is accepted (message now: `Only the dataset 'name', 'digest', and 'context' support comparison with a list of quoted string values.`) and list values are parsed with the generic list parser, not the run-id parser. (5) `search_logged_models`: the invalid-operator message lists the operators valid for the entity's type (`Expected one of {ops}`), and quoted string values are stripped exactly once. (6) `update_webhook` with `status` omitted keeps the stored status (Rust already uses `Option<i32>`; confirm via corpus and record). (7) `log_spans_async` thread offload is Python-only; confirm the OTLP/log-spans corpus stays identical.
  - **VER:** `cargo test -p mlflow-store` (new tests: link_prompts missing trace; interleaved log_batch key ordering; scorer non-integer experiment id; workspace variants where the store is workspace-aware) and `cargo test -p mlflow-search`; corpus replay `uv run --no-sync python rust/compliance/replay.py` with new cases for link-prompts-missing-trace, `list_scorers` invalid id, dataset `context IN`, logged-model bad-operator message; recorder rerun `uv run --no-sync pytest -q rust/compliance/recorders/ -k "spans or otlp"`.
  - **DONE 2026-09-01** (executor commit `c6452139b` + coordinator fix `03fd8fdc6`, ff-merged): log_batch selects the newest metric per key and upserts `latest_metrics` in sorted key order under `run_with_deadlock_retry` (`DB_WRITE_MAX_DEADLOCK_RETRIES = 2`); batch-bounded dedup is the 6-column PK `ON CONFLICT DO NOTHING` (documented in `metrics.rs`); dataset `context IN` parses via `parse_list_from_token`; logged-model operator errors list the enforced set; link-prompts existence check already present (`traces.rs:625`); webhook status omission already preserved (`webhooks.rs:151`); `36c7a7f6ec` N/A (no Python event loop). Coordinator reverted the executor's plural scorer message: Python's REST path goes through `get_experiment`, so Rust keeps `Invalid experiment ID '...'. Experiment ID must be a valid integer.` and the corpus case compares the message. VER rerun by coordinator on the rebased branch: clippy clean; `cargo test -p mlflow-store -p mlflow-search` (sqlite) green; dialect matrix `cargo test -p mlflow-store -p mlflow-registry -- --test-threads=1` green on postgres and mysql; `replay.py -k traces -k scorers -k runs -k logged_models -k webhooks` RC 0; recorder `-k "spans or otlp"` 1 passed.

- [x] T-S5 Experiment tags: 20000-char values, custom-view cap of 50 per experiment, demo custom view seed
  - **Upstream refs:** `ffa0e39ea0` [Custom View] Increase experiment tag value limit to 20000 (#25062); `a3b3f574a6` [Custom View] Enforce saved view limit per experiment (#25064); `cd13d3cdc9` [Custom View] Seed a sample custom view on the MLflow Demo experiment (#25211)
  - **Rust target:** `rust/crates/mlflow-store/src/store/{validation,experiments}.rs`, `rust/crates/mlflow-server/src/{experiments,demo}.rs`, `rust/crates/mlflow-proto` docs if the generated descriptor carries the new limit text
  - **AC:** (1) `MAX_EXPERIMENT_TAG_VAL_LENGTH = 20000`: `set_experiment_tag` and `create_experiment` accept 20000-char values and reject 20001 with Python's INVALID_PARAMETER_VALUE message; the redundant `_validate_tag` call Python dropped from `set_experiment_tag` is mirrored so error precedence matches. (2) Custom views (`MAX_CUSTOM_VIEWS_PER_EXPERIMENT = 50`, tag prefix `mlflow.customView.view`): `create_experiment` rejects a request carrying more than 50 custom-view tags; `set_experiment_tag` rejects a **new** custom-view key when the experiment already has 50 with `Unable to create another custom view for experiment {id}; the maximum number of custom views per experiment is 50. Delete an existing custom view before creating a new one.` (INVALID_PARAMETER_VALUE); overwriting an existing key is allowed. (3) Demo generation: `custom_view` is a `DemoFeature` (registered after traces, before evaluation); `POST /mlflow/demo/generate` seeds the `mlflow.customView.view.v1.mlflow-demo-span-review` experiment tag on the demo experiment with the exact payload `build_demo_custom_view()` produces (name "Span review", `created_at_ms = 1`), the feature counts in the generate/delete responses include it, and `demo/delete` removes it. Parity is byte-level on the tag value.
  - **VER:** `cargo test -p mlflow-store` (tag length + custom-view cap tests, sqlite + postgres, incl. workspace variant); corpus replay `uv run --no-sync python rust/compliance/replay.py -k "experiment or demo"` with new cases: 20000-char tag ok, 20001 rejected, 51st custom view rejected, overwrite ok, demo generate includes `custom_view`; `bash rust/e2e/run.sh` GenAI phase renders the seeded view (covered again in T-S11).
  - **DONE 2026-09-01** (executor commit `e05f37618`, ff-merged): `MAX_EXPERIMENT_TAG_VAL_LENGTH = 20000` (`validation.rs:15`); custom-view cap via `validate_custom_view_count` (create, handler + store) and `validate_custom_view_tag_write` (set, workspace-scoped existing-key lookup, overwrite allowed) with Python's two message variants; demo `custom_view` feature registered after traces, seeds/deletes `mlflow.customView.view.v1.mlflow-demo-span-review` with the byte-identical 12940-byte payload (sha256 `47a4717d…`). New corpus cases in `experiments.yaml`/`demo.yaml`, four store tests incl. workspace isolation, Playwright surface `demo-custom-view`. VER rerun by coordinator on the rebased branch: fmt/clippy clean; `cargo test -p mlflow-store` green on sqlite, `--test tracking_store` green on postgres and mysql; demo + custom_view unit tests green; `replay.py -k experiment -k demo` RC 0. Executor's GenAI e2e phase passed 17/17 incl. `demo-custom-view`; the full `run.sh` is T-S11's gate (its Part 1 phase currently fails three specs on upstream UI changes, see T-S11).

- [x] T-S6 Gateway runtime: zstd request bodies, usage extras, missing finish_reason, streaming passthrough fallback, UC model routing
  - **Upstream refs:** `eedd838ad3` Handle zstd-compressed request bodies on the AI Gateway (#24784); `ee9c6df98f` Preserve provider usage extras in OpenAI-compatible gateway (#25000); `a399ba5581` fix(gateway): preserve provider usage extras (#25010); `72760e67fc` fix(gateway): handle missing finish_reason in openai_compatible streaming (#24111); `1b231080de` Fix streaming passthrough skipping fallback (#25268); `1d84d630a8` Support UC model services in built-in judges (#25246)
  - **Rust target:** `rust/crates/mlflow-server/src/{gateway_runtime,gateway,invoke}.rs`, `rust/crates/mlflow-server/Cargo.toml` (zstd decoder), gateway differential goldens under `rust/compliance/`
  - **AC:** (1) Gateway invocation routes accept `Content-Encoding: zstd`: the body is decompressed with a cap of `MLFLOW_GATEWAY_MAX_DECOMPRESSED_REQUEST_SIZE` (default 100 MB); over the cap → 413, invalid zstd or invalid JSON after decompression → 400 with Python's messages; `content-encoding` is stripped from the egress headers so providers never see it. (2) Usage passthrough: the OpenAI-compatible adapter forwards the whole provider `usage` object (unknown keys preserved) and the Anthropic/Gemini adapters keep unknown usage keys as extras, identical to Python's serialized output. (3) OpenAI-compatible streaming frames without `finish_reason` no longer error; the field is read with `.get` semantics and emitted as null. (4) Fallback with streaming: a `stream: true` payload goes through the fallback executor (`_execute_stream_with_fallback(..., await_result=True)`), so a provider failure **before the first chunk** falls back to the next endpoint; after the first chunk is yielded there is no fallback. (5) `1d84d630a8`: Databricks model names with two or more dots route to `{host}/ai-gateway/mlflow/v1`; port if Rust's Databricks adapter resolves that base URL, otherwise record N/A with the `adapter_for` evidence.
  - **VER:** `cargo test -p mlflow-server` gateway_runtime unit tests (zstd cap/invalid, usage extras matrix per adapter, finish_reason null, pre-first-chunk fallback vs post-chunk no-fallback); recorder differentials `uv run --no-sync pytest -q rust/compliance/recorders/ -k gateway` with new streaming cases for missing finish_reason and stream fallback; corpus `uv run --no-sync python rust/compliance/replay.py -k gateway` incl. a zstd-encoded invocation case.
  - **DONE 2026-09-01** (executor commit `8e935edf9`, ff-merged): `parse_request_body` decodes `Content-Encoding: zstd` with the `MLFLOW_GATEWAY_MAX_DECOMPRESSED_REQUEST_SIZE` cap (default 100 MB; 413 over cap, 400 with Python's invalid-zstd / invalid-JSON messages) and strips the header before egress; OpenAI/Anthropic/Gemini usage extras preserved in Python's order; missing `finish_reason` already `.get`-style (coverage added); streaming passthrough now runs through `FallbackRawProviderStream` (fallback before the first emitted byte, none after); Databricks names with two or more dots route to `/ai-gateway/mlflow/v1`. `rust/tools/gateway_routing_oracle.py` now records Python's post-first-chunk stream error instead of crashing, which fixes the three `gateway_runtime_http` failures seen after the merge. New `zstd = "0.13.3"` dependency. `replay.py` gained `raw_body_base64` and runs the `gateway` section against the FastAPI app. VER rerun by coordinator on the rebased branch: fmt/clippy clean; gateway_runtime unit tests 26 passed; `--test gateway_runtime_http` 16 passed; ruff clean; recorders `-k gateway` 2 passed; `replay.py -k gateway` RC 0.

- [x] T-S7 Assistant: client-tool pause/resume, structured custom-view responses for CLI providers, tool-executor hardening, Docker sandbox for Bash
  - **Upstream refs:** `800a6ba97a` [4/N] Add client-tool pause/resume for `render_custom_view` in Assistant (#24796); `efcb31228c` [Custom View] Support local Assistant providers with structured responses (#25034); `494b735b6b` [Security] Deny Assistant Read tool file access when no project directory is configured (#24513); `0b329f8a95` Run the MLflow Assistant `Bash` tool in a hardened Docker sandbox (#25296)
  - **Rust target:** `rust/crates/mlflow-server/src/{assistant,assistant_tools,openai_compatible}.rs`, `rust/crates/mlflow-server/src/assistant_providers/`, `rust/crates/mlflow-server/tests/assistant_http.rs`, `rust/compliance/recorders/` assistant stream recorders, `dev/dev_stubs/` (extend the claude stub with `--json-schema` structured output if needed)
  - **AC:** (1) Events gain `client_tool_call` (`{request_id, tool_name, tool_input}` plus `continuation: "terminal"` only for structured providers); `error` events carry `session_id` when known and the server persists `provider_session_id` on ERROR as well as DONE. (2) New `POST /ajax-api/3.0/mlflow/assistant/sessions/{id}/tool-result` (`{request_id, content, is_error}`) stores `pending_client_tool_results` on the session (serialized alongside `pending_tool_decisions`, cleared on cancel and consumed by the next stream); the stream resumes only when a decision or a client result was delivered, and a new user message supersedes stale results. (3) The OpenAI-compatible tool loop pauses on `render_custom_view` (`CLIENT_TOOLS`) without executing or permission-gating it, emits the assistant `tool_use` message + `client_tool_call`, and on resume splices the client result as a tool message; `build_tools_schema` includes the `render_custom_view` function schema verbatim. (4) Provider info exposes `client_tool_delivery` (`"tool"` for OpenAI-compatible/gateway providers, `"structured"` for Claude Code and Codex, `"unsupported"` otherwise) in both `/providers` and the resolved-provider payload; `supports_client_tools` is not emitted. (5) When the turn context has `customTraceView`, Claude Code runs with `--json-schema CUSTOM_VIEW_RESPONSE_SCHEMA` and Codex with `--output-schema <tempfile of STRINGIFIED_CUSTOM_VIEW_RESPONSE_SCHEMA>`, the structured-output instructions are appended to the user message, assistant text blocks are suppressed from the visible stream, and the final envelope is parsed (`CustomViewResponse`, stringified-messages tolerance for a trailing `}`/`]` suffix of ≤4 chars) into a text event plus a terminal `client_tool_call`; parse failures and missing session ids produce the exact error strings; Codex `error` / `turn.failed` messages are unwrapped through `_unwrap_error_message`. `customTraceView` is turn-scoped: it is dropped from the session context when a later message omits it. (6) Tool executor hardening: non-dict tool input → `Permission denied: malformed tool input`; non-string Bash command → `Permission denied: malformed command`; `python`/`python3` in restricted mode with no project dir → `Permission denied: {argv0} requires a configured project directory`; Read/Write/Edit with a path but no cwd → `Permission denied: {tool} requires a configured project directory`; unresolvable paths → `Permission denied: malformed path {raw!r}`; restricted-mode Bash runs the validated argv directly (no shell) while full access keeps the shell; `send_message` fills a missing `working_dir` when a later message resolves a project path. Keep the existing Rust-only restrictions that are stricter than Python (documented in the DONE note). (7) Sandbox: `assistant_sandbox_enabled()` = `MLFLOW_ENABLE_ASSISTANT_SANDBOX` tri-state override, else `MLFLOW_ENABLE_REMOTE_ASSISTANT && docker on PATH`; when on, Bash runs via the docker CLI with the same hardening flags as `run_in_sandbox` (image `MLFLOW_SANDBOX_DOCKER_IMAGE` default `mlflow-sandbox:latest`, auto-built minimal image, bridge network + `host.docker.internal:host-gateway`, 1g mem/1 CPU/256 pids, read-only rootfs, cap-drop ALL, no-new-privileges, tmpfs /tmp, uid:gid, `/workspace` bind), an empty environment except loopback-rewritten `MLFLOW_TRACKING_URI`/`MLFLOW_REGISTRY_URI` without embedded credentials (`to_container_host_uri`), 120 s timeout → `Command timed out after 120 seconds`, unavailable docker → `Sandbox is enabled but the command could not be run: {e}` with **no** host fallback.
  - **VER:** `cargo test -p mlflow-server --test assistant_http` + unit tests (tool_executor denial matrix, `to_container_host_uri` table, custom-view envelope parsing incl. stringified suffix cases, `_unwrap_error_message`); recorder differentials `uv run --no-sync pytest -q rust/compliance/recorders/ -k assistant` with a new client-tool pause/resume recording and a structured custom-view recording against the claude stub; `bash rust/e2e/run.sh` assistant phase (T-S11) with `--stub-providers claude`.
  - **DONE 2026-09-01** (executor commit `7c992b419` + coordinator `0062a8fe1`, ff-merged): `client_tool_call` events (`continuation: terminal` for structured providers), `error` events carry `session_id` and ERROR persists `provider_session_id`; `POST /sessions/{id}/tool-result` stores `pending_client_tool_results` (cleared on cancel, consumed by the next stream, superseded by a new message); OpenAI-compatible loop pauses on `render_custom_view` and splices the client result on resume; `client_tool_delivery` in `/providers` and the resolved provider; Claude Code `--json-schema` / Codex `--output-schema` structured custom views (`assistant_custom_view.rs`); tool-executor denial matrix; Docker sandbox argv matches `mlflow/server/sandbox/container.py` (bridge, host-gateway, 1g/1g, 1 cpu, 256 pids, read-only, cap-drop ALL, no-new-privileges, tmpfs /tmp, uid:gid), no host fallback. `dev/dev_stubs/claude_cli.py` gained `--json-schema` structured output (coordinator restored its docstring). VER rerun by coordinator on the rebased branch: fmt/clippy clean; `cargo test -p mlflow-server --lib` 240 passed; `--test assistant_http` 8 passed; ruff clean; recorders `-k assistant` 40 passed (new client-tool pause/resume, OpenAI resume, and Claude-stub structured custom-view recordings). e2e assistant phase runs under T-S11.

- [x] T-S8 Static prefix for native routers; CLI/worker no-ops verified
  - **Upstream refs:** `e80938dbc8` fix: Serve FastAPI-native routers under `--static-prefix` (#24511); `b5354717fb` Add `--artifact-policy retarget` to `mlflow db move-resources` (#24635) (N/A); `19ea186d46` fix: use chunked I/O in HdfsArtifactRepository (#21941) (N/A); `254c4e387c` feat: add rate limiting and retry support to automatic evaluation path (#24702) and `df8df41036` Propagate eval_retry_context flags into the scorer thread pool (#25318) (Python worker)
  - **Rust target:** `rust/crates/mlflow-server/src/{config,lib,routes,assistant,security}.rs`, `rust/crates/mlflow-server/src/auth_middleware/mod.rs`, `rust/crates/mlflow-server/tests/static_prefix*.rs`
  - **AC:** With `--static-prefix /p`: assistant, jobs, gateway runtime, OTLP, and every other native router are served under `/p/...` and not at the bare path; the assistant `stream_url` is prefixed (`get_server_base_url` / `_add_static_prefix`); a prefix containing `{` or `}` is rejected at startup with Python's message; the auth middleware strips the prefix before dispatch; the gateway timing middleware and `is_api_endpoint` recognize prefixed and unprefixed artifact routes. `b5354717fb` is a Python CLI over the workspace store (Rust has no `mlflow db` CLI; N/A). `19ea186d46` is HDFS, unsupported in Rust (N/A). `254c4e387c`/`df8df41036` change the online-scoring evaluation path that runs inside the Python worker subprocess (plan D14); confirm the Rust scheduler passes the new env (`MLFLOW_GENAI_EVAL_PREDICT_RATE_LIMIT`, `MLFLOW_GENAI_EVAL_MAX_RETRIES`, `MLFLOW_GENAI_EVAL_SCORER_RATE_LIMIT`) through untouched and no Rust reimplementation exists.
  - **VER:** `cargo test -p mlflow-server` static-prefix tests (new cases for assistant stream_url, jobs, gateway, OTLP under a prefix; `{`/`}` rejection); `uv run --no-sync python rust/compliance/replay.py -k static_prefix` (or the full replay) green; N/A items evidenced in the DONE entry.
  - **DONE 2026-09-01** (executor commit `024996719`, rebased onto `fa7b3c8a7`): `StaticPrefix` extension in `lib.rs` nests the Flask-compatible routes and every native router under the prefix while keeping the artifact proxy at its bare path (Python keeps `/api/2.0/mlflow-artifacts/...` unprefixed); assistant `stream_url`/tracking URI prefixed; auth middleware strips the prefix before dispatch (`basic_credentials` takes the routed path); `is_api_endpoint` accepts prefixed API paths plus bare/ajax artifact paths; `--static-prefix` with `{`/`}` fails with Python's message. New `tests/static_prefix_http.rs` (jobs, assistant, gateway, OTLP, stream_url, auth dispatch, gateway timing header, artifact routes) and CLI brace-rejection cases; corpus section `static_prefix` (5 cases, both servers booted with `--static-prefix /p`, `replay.py` gained per-section `static_prefix`). N/A evidence: `b5354717fb` needs an `mlflow db` CLI, Rust's `main.rs` only launches the server; `19ea186d46` is HDFS, `mlflow-artifacts` has no HDFS adapter; `254c4e387c`/`df8df41036` reach the Python worker unfiltered (`mlflow-genai/src/worker.rs` inherits the environment, `native_worker.rs` removes only workspace/tracking credentials), but the Rust native online-scoring path also reimplements the harness, so that parity gap was split out as T-S13. Coordinator VER on the rebased branch: fmt/clippy clean, `cargo test -p mlflow-server --no-fail-fast` 843 passed / 0 failed, replay `-k static_prefix` 5 cases, 0 diffs.

- [x] T-S9 Artifacts: sibling-path deletion guard and S3 directory markers
  - **Upstream refs:** `6af9543433` Prevent object-store artifact deletion from removing sibling paths (#25024); `193a2364a0` Ignore S3 directory markers in artifact listing (#23250)
  - **Rust target:** `rust/crates/mlflow-artifacts/src/{repo,s3}.rs`, `rust/crates/mlflow-artifacts/tests/s3_minio.rs`
  - **AC:** (1) Deleting artifact path `foo` on an object store deletes `foo` and `foo/...` only, never `foobar` (`_is_object_key_within_path`: exact match or `/` boundary; a trailing-slash path is a pure prefix; empty path is the root). (2) Listing under S3 skips zero-byte keys ending in `/` (directory markers) so they never appear as files; sizes and ordering otherwise unchanged. Both apply to the local object-store repo and the S3 repo.
  - **VER:** `cargo test -p mlflow-artifacts` (unit tests for the boundary helper and marker filter) and the MinIO integration test `cargo test -p mlflow-artifacts --test s3_minio -- --include-ignored` on the docker compose MinIO; corpus `uv run --no-sync python rust/compliance/replay.py -k artifact` green.
  - **DONE 2026-09-01** (executor commit `a1ebb1da6`, rebased onto `0d0bd4311`): `is_object_key_within_path` / `file_is_directory_marker` mirror the Python helpers; `ObjectStoreRepo::delete` filters listed keys on the `/` boundary and probes the exact object separately (trailing slash = pure prefix); the S3 repo lists raw `<prefix>/` marker keys and deletes them after the object-store delete. Coordinator VER (rebased worktree): fmt/clippy clean, `cargo test -p mlflow-artifacts` 39+1 passed, `--features aws --lib` 46 passed, MinIO `s3_minio --include-ignored` 4 passed (MinIO from `rust/bench/docker-compose.soak.yml`), replay `-k artifact` 36 cases, 0 non-allowlisted diffs (3 pre-existing allowlisted ETag diffs).

- [x] T-S10 Model catalog refresh and verify-only server-api commits
  - **Upstream refs:** catalogs `7b6052ff46`, `d52ad42b1a`, `b8f1e7385b`, `6ffc1275d7`, `5f4d7d2071`, `3c7e67c1ef`, `b7fd564aec`, `0a84a9bc0f`, `a997ac0f31`, `5ebb20c256` (Update model catalog from upstream sources / max_output_tokens fixes); verify-only `3cbd43dc66` immutable evaluation dataset versions (#24845, `databricks_rest_store` only), `08eb62e14f` Databricks scorer versioning (#24940, Databricks store + client sample-rate validation), `403abbd24a` `merge_records` IndexError (#25498, client entity), `8c9cc991aa` expired experimental decorators (#24807), `c2dcdb96c6` `generate_rationale_first` (#25002), `1b28ec40a8` MemAlign judges (#24883), `c3853edc08` chat-type prompts in `optimize_prompts` (#23488), `082588fe6d` ensemble experimental version (#24914), `6bc06f1221` docstring Args (#24925), `525b57c481` V4/UC trace IDs in `LiveSpan.add_link` (#25097), `512ac87b60` `mlflow scorers list --builtin` (#25003), `5d46e84c12` GenAI docs (#25164), `f847d01ff1` GEPA datasets (#25387), `948322ac3f` mypy strict harness (#25309)
  - **Rust target:** `rust/crates/mlflow-server/build.rs` catalog embedding from merged `mlflow/utils/model_catalog/*.json`, `rust/crates/mlflow-server/src/gateway_provider_matrix.rs` goldens, corpus goldens for supported-models/server-info
  - **AC:** Rust-served supported-models / provider-config data equals the merged end state of all ten catalog commits after a rebuild (build.rs re-embeds the JSON; goldens re-pinned if sizes shift). Every verify-only commit is client/SDK/Databricks-only or docs/typing: the corpus and conformance matrix show zero diffs attributable to them. Record one line per commit in the DONE note.
  - **VER:** `cargo build -p mlflow-server` then `cargo test -p mlflow-server --test gateway_discovery_http`; `uv run --no-sync python rust/compliance/replay.py -k "supported_models or server_info or gateway"`; full `uv run --no-sync python rust/genai-inventory/run_conformance.py --profile required` green.
  - **DONE 2026-09-01** (executor commit `23b3caf53`, rebased onto `f930cdde8`): seven supported-models discovery goldens re-pinned to the rebuilt catalog (only sizes/hashes changed, e.g. 1045→1066 bytes for the provider list), GenAI inventory ledger/scorers/algorithms and `REPORT.md` regenerated against the merged Python tree (this also cleared the `validate_ledger.py` drift T-S2 hit), `t22_2_required` report refreshed. Catalog commits `7b6052ff46`, `d52ad42b1a`, `b8f1e7385b`, `6ffc1275d7`, `5f4d7d2071`, `3c7e67c1ef`, `b7fd564aec`, `0a84a9bc0f`, `a997ac0f31`, `5ebb20c256`: all re-embedded by `build.rs` from the merged `mlflow/utils/model_catalog/*.json`. Verify-only, N/A for Rust: `3cbd43dc66` (dataset version is client/Databricks state; `datasets.proto` has no version field), `08eb62e14f` (only `mlflow/genai/scorers/{base,registry}.py`; Rust sample-rate validation unchanged), `403abbd24a` (`merge_records` in the Python entity), `8c9cc991aa` (removed experimental decorators), `c2dcdb96c6` (Python judge construction), `1b28ec40a8` (MemAlign optimizer code), `c3853edc08` (`optimize_prompts` chat prompts), `082588fe6d` (experimental-version metadata), `6bc06f1221` (docstrings), `525b57c481` (`LiveSpan.add_link` client entity; no request/response schema change), `512ac87b60` (`mlflow scorers list --builtin` CLI), `5d46e84c12` (docs), `f847d01ff1` (GEPA local dataset construction), `948322ac3f` (typing/mypy). Coordinator VER: `gateway_discovery_http` + `gateway_runtime_http` green (discovery rerun 7 passed on the rebased branch), replay `-k gateway -k server_info -k supported` 92 cases, 0 diffs (first run showed one diff only because the venv lacked `zstandard` from `requirements/test-requirements.txt`; installed), `run_conformance.py --profile required` on the main venv: sqlite + postgres 3+11 / 3+11 passed, auth suite 11 passed.

- [x] T-S11 Rebuild the upstream UI and run UI smoke
  - **Upstream refs:** all 44 `ui`-bucket commits (Traces V4 port + mount as default `c7704b3524`, `c71de312d3`, `0c87604862`, `f5c94ec78c`, `6ff2cc0eff`, `0369e7437f`, `a943e470dd`; trace explorer v2 `3c0b3bab55`; custom view UI `bec7c82ce6`, `72a49e30cd`, `f64c342ee0`, `c974385215`, `26a8db3209`, `627d1ebd02`, `4db07ecb45`, `400d17463d`; assistant UI `f95d39af48`, `b4d501697c`, `43d7569e8e`; componentId registry move `e4613691c0`; experiment tag limits `ad5f290a73`; version bump `ec8b88ac64` to 3.15.2.dev0; charts/compare/run-page fixes `6d9cc812b5`, `d4aa4ec9ca`, `095700713a`, `66f11a3239`, `687ab22f0e`, `023c4f9bf3`, `7d1f3e2ab4`, `38a37ef23f`, `6267ac12d6`, `90535ef65a`, `96ac0d31c3`, `b499b4c2f5`, `9716b57ceb`, `52487cbe81`, `1985d5ae10`, `6c821492e3`, `71d295caa5`, `fd611bed2e`, `dbd24aba9b`, `8cce72ce86`, `b7ac8a6733`, `4caf49218e`) plus the server-api-bucket `f3d8ed3951` Restyle Traces V4 saved views (#25407), whose saved-view envelope is a UI-authored experiment-tag contract persisted through the existing tag API
  - **Rust target:** production React static build served by the Rust deployment
  - **AC:** The changed UI builds and all affected surfaces work against Rust without Python-attributed responses: Traces V4 as the default traces page (saved views persisted via experiment tags, exact trace-id search footer), trace explorer v2, custom view tab (create/rename/delete/feedback against the T-S5 cap and T-S7 assistant flow), run pages, and the assistant floating button under `MLFLOW_ENABLE_REMOTE_ASSISTANT`.
  - **VER:** `bash rust/e2e/run.sh`
  - **DONE 2026-09-01** (coordinator): production UI rebuilt from the merged `mlflow/server/js` in Phase 1; `bash rust/e2e/run.sh` green from head `296cfa1b4` (genai 17 passed, part1 16 passed, auth 1 passed, in both the auth-disabled and auth rounds, `E2E_RC=0`). Spec updates for the merged UI in `rust/e2e/tests/part1-ui-smoke.spec.mjs`: Traces V4 defaults to a 7-day window, so the seeded 2026-07-20 traces are opened with `startTimeLabel=ALL`; the Detect Issues onboarding card is suppressed via `mlflow.detectIssues.guidanceShown_v1` in an init script; the trace detail page now renders the v4 drawer, where the span tree shows without a `Details & Timeline` tab and assessments sit behind the `Assess trace (N assessments)` toggle. Evidence reports `rust/compliance/report/t11_6_ui_smoke.md` and `t22_5_ui_smoke.md` regenerated by the run (17/17 surfaces incl. the T-S5 demo custom view).

- [x] T-S12 Version and environment pin parity after the upstream version bump
  - **Upstream refs:** `ec8b88ac6` Update version to 3.15.2.dev0 (#25361) → `mlflow/version.py` now `3.15.3.dev0`; the phase-1 `uv sync` also moved the venv to pip 26.2.1 and wheel 0.48.0, which Python's `_PythonEnv.current()` writes into promptlab `conda.yaml`/`python_env.yaml`
  - **Rust target:** `rust/crates/mlflow-genai/src/payload.rs` (`PINNED_MLFLOW_VERSION`), `rust/crates/mlflow-genai/tests/{payload,phase19_contract}.rs`, `rust/crates/mlflow-server/src/promptlab.rs` (`conda_yaml`, `python_env_yaml`)
  - **AC:** Rust promptlab models are byte-identical to Python's (`rust/tools/promptlab_cross_language.py`), and scorer payloads default `mlflow_version` to the merged Python version.
  - **VER:** `cargo test -p mlflow-server --test promptlab_http -p mlflow-genai --test payload --test phase19_contract`.
  - **DONE 2026-09-01** (coordinator, commit `b28c52490`): pins moved to `3.15.3.dev0`, `pip<=26.2.1`/`pip==26.2.1`, `wheel==0.48.0`; VER 3+6+3 tests green, `cargo fmt --check` and clippy clean.

- [x] T-S13 Online scoring: scorer rate limit and retries from `MLFLOW_GENAI_EVAL_*` (found during T-S8 review)
  - **Upstream refs:** `254c4e387c` feat: add rate limiting and retry support to automatic evaluation path (#24702); `df8df41036` Propagate eval_retry_context flags into the scorer thread pool (#25318). The T-S8 plan text assumed these only reach the Python worker; the Rust native online-scoring path (`mlflow-genai/src/online.rs` `score_and_log`) reimplements the evaluation harness and hardcoded `max_retries: 0` with no scorer rate limit.
  - **Rust target:** `rust/crates/mlflow-genai/src/online.rs`, `rust/crates/mlflow-genai/src/evaluation.rs` (`EvaluationConfig::from_env`)
  - **AC:** Online scoring derives its scorer rate limit the way `trace_processor.py` now does: `MLFLOW_GENAI_EVAL_SCORER_RATE_LIMIT` when set, else `MLFLOW_GENAI_EVAL_PREDICT_RATE_LIMIT` scaled by the number of distinct scorers in the tick (`_get_scorer_rate_config`), and `max_retries` comes from `MLFLOW_GENAI_EVAL_MAX_RETRIES` (default 3). `df8df41036` is a Python threading detail (contextvars copied into the scorer pool); the Rust engine passes `max_retries` to each scorer call directly, so no port is needed beyond the config.
  - **VER:** `cargo test -p mlflow-genai online` (new unit test for the online config derivation) and `cargo test -p mlflow-genai --test` suites touching `EvaluationConfig`; `cargo fmt --check` and clippy clean.
  - **DONE 2026-09-01** (coordinator, commit `322760ae7`): `score_and_log` now builds its config from `EvaluationConfig::from_env(distinct scorer count)` via `online_evaluation_config` (row_workers stays 1, scorer tracing still off inside an online session). Unit test `online_config_keeps_env_rate_and_retries_but_scores_one_row`; full `cargo test -p mlflow-genai` 63 passed, fmt and clippy (`-D warnings`) clean.

## Skipped

- `client-sdk` (30): `5bccdc775b`, `352b251578`, `76bde43d80`, `040056f186`, `c46e5031f8`, `e6713e4b97`, `0e7cb0d7ca`, `b4c80ec59d`, `4c30a157a7`, `fedd4ad8a7`, `feeb0f76b4`, `724b88c8a3`, `2f4df21db2`, `6007df0dd4`, `988d28e4d9`, `24b8cc7c82`, `75ef028bfd`, `67ed0bee3d`, `af7acb646c`, `4d5280fba0`, `ac9005c2d5`, `0d917bf205`, `68ef051178`, `2368f50213`, `6363c26181`, `479bee3e3d`, `0572b16ac9`, `f9af18eda7`, `36dace470a`, `89a0b7efc7` — autologging (pydantic-ai, langchain, openai, dspy), pyfunc/transformers/onnx flavors, scoring server, tracing client export/flush, cross-version test pins; not ported — the Python client, SDK, autologging, and flavors remain the client implementation.
- `infra` (172 minus the four pulled into tasks): CI/workflow maintenance (review bot, NaiLaOpus, gVisor review sandbox, gateway-routed review, dependabot, `uv.lock` refreshes), Claude/Codex skills and plugin changes, docs (span links, hosting options, Kubernetes auth, examples), TypeScript SDK (`223aaaa4e3`, `f28ad5c4ab`, `43f63217c1`, `50d4736d74`, `909429cbd2`), Helm chart (`2e032f1c4f`, `75dea15e2a`, `287d40a339`), Docker base image, flaky-test annotations and test-only fixes, changelog for 3.15.2 — merged as upstream maintenance with no Rust behavior to port. `847fca8031`, `e6a3c71309`, `c3bfb448d4`, `cd13d3cdc9`, `ec8b88ac6` are handled in T-S2, T-S4, T-S4, T-S5, T-S12 respectively.

## Completion checklist

- [x] Unary differential corpus replay is green:
      `uv run --no-sync python rust/compliance/replay.py`.
      Evidence 2026-09-01 (final head, main venv): 554 cases, 0 non-allowlisted diffs, 16 allowlisted, 0 status mismatches, 0 errors; auth 67 + auth_artifacts_only 2 ran (no skipped sections); new sections this sync: jobs 7, static_prefix 5, demo 6.
- [x] Required Python-over-HTTP conformance matrix is green:
      `uv run --no-sync python rust/genai-inventory/run_conformance.py --profile required`.
      Evidence 2026-09-01: coverage `complete` on sqlite and postgres (`MLFLOW_RUST_CONFORMANCE_PG_URI`), auth suite 17/17 (`rust/compliance/report/ts2_basic_auth.md`), report `rust/compliance/report/t22_2_required.md`.
- [x] SSE/streaming recorder differentials are green:
      `uv run --no-sync pytest -q rust/compliance/recorders/`.
      Evidence 2026-09-01: 42 passed.
- [x] Three-phase Playwright UI smoke is green: `bash rust/e2e/run.sh`.
      Evidence 2026-09-01: genai 17, part1 16, auth 1 passed in both the auth-disabled and auth rounds (`E2E_RC=0`).
- [x] Production UI was rebuilt if the `ui` bucket was non-empty. (Phase 1 rebuild; T-S11.)
- [x] New upstream endpoints have new corpus/conformance cases, not code-only coverage. (`jobs.yaml`, `static_prefix.yaml`, `demo`/custom-view cases, 30 new `auth.yaml` cases, `basic_auth.yaml` conformance rows, auth Playwright spec.)
- [x] `rust/sync/state.json` advances to `98209f58fe27c124f0cd28a69e1b3ea80ba0d804` and records this plan.
