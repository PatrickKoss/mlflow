# Rust upstream sync plan — 2026-08-02

Status: **OPEN** · From: `9f872c10cd88eaa062e612b6726b06cedbd75084` · To: `0489fb31d8a144ec3c80d15eb44dbe24b790465e`

| Bucket       | Commits |
| ------------ | ------: |
| `server-api` |      45 |
| `ui`         |      30 |
| `client-sdk` |      11 |
| `infra`      |      53 |

Upstream merge commit: `0b78cebff` (merged 2026-08-02; four conflicts resolved by
responsibility: fork ADK CI install + upstream `$/` action syntax in
`.github/workflows/master.yml`, fork conformance `db_uri` redirect + upstream mkdtemp
cleanup in `tests/conftest.py`, fork Rust auth-server launch + upstream served-artifacts
env defaults and flaky marker in `tests/server/auth/test_auth.py`, both import sets in
`ExperimentPageTabs.tsx`).

When in doubt, the merged upstream Python implementation is the behavioral spec.

## Tasks

- [x] T-S1 MCP registry net-state parity (envelopes, tri-state tools, icons, connect_options, allowed_actions, no version display_name)
  - **DONE 2026-08-02:** commit `d99475bdf` (executor: Codex, rebased onto post-T-S4 head; coordinator-verified). Executor ran MCP conformance sqlite+postgres 67/67 each, zero diffs. Independent coordinator VER rerun after rebase: full `cargo test -p mlflow-store` green (incl. the 2 MCP tests that failed pre-T-S1), `cargo test -p mlflow-auth` green, `mcp_registry_http` + `auth_after_request_no_default_http` green, corpus replay `-k mcp_server_registry -k auth` rc=0. Telemetry AC item recorded as explicit N/A in the commit message (Rust server has no store usage-event pipeline). Integrated ff-only.
  - **Upstream refs:** `045bceb7f6` Auto-discover MCP tools on create when tools omitted (#24520); `7cdf9eee5e` Accept MCP registry server envelopes (#24572); `80c376037e` Telemetry events for MCP registry operations (#24477); `c4a3fe486d` Return resolved MCP server icons on list and get (#24645); `61be62ecda` Access endpoints, RBAC gating, compare view (#24509); `df9dbcc50b` Remove display_name from MCP server versions; `ef48e31688` MCP server detail page, version management, aliases, tags (#24298)
  - **Rust target:** `rust/crates/mlflow-server/src/mcp_registry.rs`, `rust/crates/mlflow-store/src/store/mcp_registry.rs` + `rust/crates/mlflow-store/src/schema/mcp.rs` (migration `a8b9c0d1e2f3` was edited in place upstream: `mcp_server_versions` gains `connect_options` JSON NULL, loses `display_name`), `rust/crates/mlflow-auth` (allowed_actions injection + endpoint-keyed response filters)
  - **AC:** Port the merged **end state**, not intermediate commits: (1) create-version accepts `server_json` as raw shape or `{"server": {...}}` envelope with upstream's exact unwrap precedence and 400 on non-dict `server`; (2) `tools` on create is tri-state (omitted → NULL, explicit null → NULL, list → stored; no server-side discovery); (3) `MCPServerResponse.icons` resolution: server icons override (empty list = explicit no-icons), latest-version `server_json.icons` backfill deduped by theme with `source: "server"|"version"` annotation; writes reject `source:"version"` with INVALID_PARAMETER_VALUE (exact upstream message) and silently strip `source:"server"`; (4) version rows/requests/responses have `connect_options: {key: {hidden: bool}}` with omitted/null/replace update semantics and **no** `display_name` anywhere (unknown `display_name` in requests silently ignored); (5) basic-auth responses stamp `allowed_actions` (USE/UPDATE/DELETE/MANAGE order) on search **and** single get for non-admins only, matching the endpoint-function-keyed filter rework; (6) MCP registry telemetry events `mcp_registry_create_server_version` (status, has_source, num_tools nullable, has_remotes) and `mcp_registry_create_access_endpoint` (transport_type, uses_alias) fire from the store layer iff the Rust server emits store telemetry (record explicit N/A if telemetry is Python-only).
  - **VER:** Extend the `mcp_server_registry` conformance suite in `rust/genai-inventory/run_conformance.py` (sqlite+postgres) with cases: envelope unwrap (raw, enveloped, non-dict server → 400, missing name/version passthrough), tri-state tools, icon resolution matrix (server-only, version-backfill, empty-list override, theme dedupe), `source:"version"` write rejection, connect_options CRUD semantics, absent display_name; unary corpus cases for the new error paths; auth conformance rows for `allowed_actions` on get + search (admin vs non-admin).

- [ ] T-S2 Presigned download endpoint + native artifact serving parity
  - **Upstream refs:** `4cf9613795` createPresignedDownloadUrl tracking endpoint (#24435); `e2727ea6e3` Advertise presigned capabilities via /server-info (#24341); `5bac866bf1` Enforce artifact authorization on presigned download route (#24571); `8e67441d53` Native FastAPI artifact upload/download with streaming (#24340)
  - **Rust target:** `rust/crates/mlflow-server/src/artifacts.rs` + `rust/crates/mlflow-artifacts`, `rust/crates/mlflow-server/src/server_info` path (wherever `/server-info` is built), `rust/crates/mlflow-auth` (proxy-artifact path matching incl. static-prefix variants and method→permission map)
  - **AC:** (1) `POST /api/2.0/mlflow/artifacts/presigned-download-url` (+ ajax + static-prefix) exists with upstream's exact validation order: required/string `run_id`+`path`, path safety, `1 <= expiration <= 604800` (also for the `MLFLOW_PRESIGNED_DOWNLOAD_URL_TTL_SECONDS` default, default 300) → INVALID_PARAMETER_VALUE; proxied-artifact run URIs (http/https/mlflow-artifacts) → INVALID_PARAMETER_VALUE; non-presignable repo → NOT_IMPLEMENTED; 503 in `--artifacts-only`; basic-auth gate = `validate_can_read_run`; response `{presigned_url, headers?, file_size?}`. (2) `/server-info` gains `multipart_uploads_enabled`/`multipart_downloads_enabled`, both false unless serving proxied artifacts and the repo supports MPU/MPD, never 5xx on resolution failure. (3) `/mlflow-artifacts/presigned/` (api+ajax+static-prefixed) requires experiment READ via the artifact-proxy permission path. (4) Streaming GET/PUT on `/mlflow-artifacts/artifacts/*` matches upstream semantics: 503 gates with upstream detail strings, 400 on directory GET and on PUT path ending in `/`, `Content-Disposition: attachment`, `X-Content-Type-Options: nosniff`, empty-200 PUT, no partial artifacts visible to readers; method→permission map GET→read, PUT→update, DELETE→manage, POST→update; experiment-id extraction covers `mpu/create|complete|abort` and `?path=` fallback.
  - **VER:** New unary corpus cases in `rust/compliance/` for presigned-download-url (happy path on a presignable backend if the corpus MinIO stack supports it, plus every error path) and `/server-info` field matrix; auth conformance rows for presigned-route permission enforcement; recorder/e2e artifact upload-download round-trip stays green (`uv run --no-sync pytest -q rust/compliance/recorders/` artifact cases + `bash rust/e2e/run.sh`).

- [ ] T-S3 Artifacts-only mode with workspaces + default-workspace grant resolution
  - **Upstream refs:** `9419fd35bf` Enable artifacts-only mode with workspaces (#24452); `b6d31aacb8` Honor experiment grants on --artifacts-only servers when workspaces disabled (#24585)
  - **Rust target:** `rust/crates/mlflow-server` CLI/config validation + handler gating, `rust/crates/mlflow-auth` permission resolution, workspace scoping of the presigned proxy route
  - **AC:** (1) `--artifacts-only --enable-workspaces` is accepted iff `--workspace-store-uri` is given (else the upstream UsageError text) and initializes only the workspace store; (2) workspace CRUD routes, `/get-artifact`, and `/upload-artifact` return the upstream 503 plain-text body in artifacts-only mode; (3) `/mlflow-artifacts/presigned/<path>` applies workspace scoping identically to the proxy download path; (4) with workspaces disabled, permission resolution consults grants in the default workspace before falling back to the config default (grants no longer silently ignored on artifacts-only servers).
  - **VER:** Conformance rows for the CLI validation and 503 matrix (artifacts-only × workspaces on/off); auth conformance case proving a default-workspace experiment grant is honored when workspaces are disabled; unary corpus 503-body cases.

- [x] T-S4 Tracking-store migrations + span-search and metric-sampling parity
  - **DONE 2026-08-02:** commit `62e2faceb` (executor: Codex, coordinator-verified). Independent VER rerun in `ts4-store` worktree: `cargo test -p mlflow-store` — all suites green except the 2 pre-existing MCP tests owned by T-S1 (removed `display_name` column, fixed by T-S1's branch); `cargo test -p mlflow-registry` green; `cargo test -p mlflow-server --test metrics_http --test registry_http` 25+25 green; corpus replay sections metrics(14)/traces(17)/registry(30) — 61 cases, 0 non-allowlisted diffs (the 30 failing `mcp_server_registry` cases are T-S1 scope, base predates its fix). Schema anchor advanced to `6f8d9c3b2a1e`; `target_value: None` placeholder in gateway.rs left neutral for T-S6. Integrated ff-only.
  - **Upstream refs:** `7d4226d879` Span text-search efficiency (#24377, migration `c1d2e3f4a5b6`); `cae618f1ba` Sample metric history by row in get-history-bulk-interval (#24305); `3f5172a9b8` Idempotent default experiment (#24598); `be138ab308` Experiment GC FK cascades (#24351, migration `6f8d9c3b2a1e`); `ab16e5cfeb` Model version string→int cast (#24565)
  - **Rust target:** `rust/crates/mlflow-store/src/db.rs` schema anchor + DDL parity (drop `index_spans_experiment_id`, add `index_spans_experiment_id_start_time`; `fk_trace_info_experiment_id` gains ON DELETE CASCADE), `rust/crates/mlflow-search` + trace search SQL generation, `rust/crates/mlflow-store/src/store/metrics_bulk.rs`, default-experiment bootstrap, `rust/crates/mlflow-registry` version parsing
  - **AC:** (1) Alembic head consumed by Rust advances through `c1d2e3f4a5b6` → `c3d9e7f1a2b4` → `6f8d9c3b2a1e` (see T-S6 for `c3d9e7f1a2b4`); span filters compile to a correlated EXISTS with `spans.experiment_id = trace_info.experiment_id` correlation and the 10 000 ms timestamp-skew lower bound (`start_time_unix_nano >= (N-10000)*1e6`) derived from trace `timestamp_ms >/>=` clauses — same rows returned, including the skew-tolerance edge; `locations=None` no longer filtered. (2) get-history-bulk-interval: lone `start_step`/`end_step` → 400 with upstream's SQLAlchemy-store message; per-run row sampling with canonical `(step, timestamp, value, is_nan)` ordering, NTILE-first-of-bucket + forced-global-last-row (append if short, else replace last), `max(1, max_results)` clamp, NaN materialization — point sets match Python exactly on sqlite+postgres. (3) Default-experiment creation is idempotent (unique-violation swallowed iff experiment 0 exists) including renamed-default and workspace-store startup. (4) Registry version params normalize like Python (`"03"`, `" 3 "` accepted; non-integers → INVALID_PARAMETER_VALUE with `not_integer_value` wording) — verify existing Rust behavior and add coverage rather than assume.
  - **VER:** Corpus replay cases: span-filtered SearchTraces incl. a span 9.9 s older than the trace-timestamp bound (matches) and 10.1 s older (excluded); get-history-bulk-interval differential cases (dense duplicate steps at step 0, sparse runs, lone-step-param 400) on sqlite+postgres via conformance; Rust store test for double-start default-experiment bootstrap with renamed default; registry version-cast conformance rows. Schema diff check: Rust-created schema matches merged `tests/resources/db/latest_schema.sql` for the touched tables/indexes/FKs.

- [ ] T-S5 Server-side scorer/issue-detection gates + online-scoring auth validators
  - **Upstream refs:** `991f39110d` Server-side check for invoking custom scorer (#24540); `bf49dd9783` Issue-detection modal handler changes (#24579); `a41f996147` Enforce experiment permission on online scoring config endpoints (#24562); `a48f7e2e0f` Fix issue detection structured output schema (#24674); `4c1f89ce06` MemAlign guideline distillation strict schemas (#24547)
  - **Rust target:** `rust/crates/mlflow-server/src/invoke.rs` + `issues.rs` + `online_scoring_scheduler.rs`, `rust/crates/mlflow-auth` route validators, `rust/crates/mlflow-genai/src/discovery.rs` + `memory.rs` + `judge.rs` schema builders
  - **AC:** (1) `/mlflow/scorers/invoke`: `serialized_scorer` must parse as JSON (else 400 "serialized_scorer must be valid JSON") and any non-null `call_source` → 400 with the DECORATOR_SCORER_REGISTRATION_NOT_SUPPORTED_ERROR message — never reconstruct attacker-supplied source. (2) `/mlflow/issues/invoke`: provider lowercased for credential lookup and recorded model name; unknown provider → 400 echoing original-case provider; no endpoint/secret and no matching env var on the server → 400 with upstream's exact hint wording. (3) Basic-auth gates on `GET /mlflow/scorers/online-configs` (REST+ajax: read `scorer_ids` from query or body, resolve configs via store, require can_read on every experiment) and `PUT /mlflow/scorers/online-config` (experiment_id from body, require can_update, deny when missing). (4) Issue-detection structured-output schema: `example_indices` and `category_rationale` required (empty-string convention), and the strict-schema builder strips all `$ref` siblings; MemAlign distillation (if the Rust optimization job builds these schemas): inlined `$defs`, `additionalProperties: false`, complete `required`, unstructured-request fallback on structured-output failure — record explicit N/A per surface the Rust worker does not own.
  - **VER:** Unary corpus cases for both invoke gates (invalid JSON, call_source set, provider-case matrix, missing-credential 400s); auth conformance rows for online-scoring config endpoints (read/update allowed/denied, scorer_ids in body vs query); Rust genai unit tests asserting emitted JSON schemas byte-match Python's for the discovery + memalign payloads (or documented N/A).

- [ ] T-S6 Gateway per-endpoint budget policies
  - **Upstream refs:** `3f0c34e9ac` Support per-endpoint budget policies in the AI Gateway (#24370; proto `BudgetTargetScope.ENDPOINT`, `target_value` fields, migration `c3d9e7f1a2b4`)
  - **Rust target:** `rust/crates/mlflow-proto` regeneration; `rust/crates/mlflow-store/src/store/gateway.rs` (`budget_policies.target_value` column + `idx_budget_policies_target_value`, normalization, endpoint existence check, `sum_gateway_trace_cost` endpoint filter); `rust/crates/mlflow-server/src/budget.rs` (`_policy_applies` with ENDPOINT scope, tracker record/reject signatures, backfill) + every gateway route call site in `gateway.rs`/`gateway_runtime.rs`; `rust/crates/mlflow-webhooks/src/payloads.rs` (`target_value` field, `ENDPOINT` scope)
  - **AC:** (1) Create/Update validate exactly like upstream: ENDPOINT requires non-empty `target_value` ("target_value is required when target_scope is ENDPOINT."), non-ENDPOINT with target_value → 400 ("target_value can only be set when target_scope is ENDPOINT."), update validates the effective post-merge state with has-field semantics and drops the old target_value on scope change; ENDPOINT target must reference an existing gateway endpoint else RESOURCE_DOES_NOT_EXIST; store-level normalization silently drops target_value for non-ENDPOINT scopes. (2) Enforcement: ENDPOINT policies match on endpoint_id ignoring workspace, GLOBAL always, workspace otherwise; enforcement + cost recording wired through all gateway routes (unified, OpenAI passthrough incl. responses+stream, Anthropic messages, Gemini generate/streamGenerate, raw proxy); new ENDPOINT policies backfill historical spend from that endpoint's traces. (3) `_list_budget_windows`: GLOBAL always shown, else policy.workspace match. (4) Budget-exceeded webhook payload carries `target_value` and widened scope.
  - **VER:** Unary corpus cases for budget-policy CRUD validation matrix (all four 400/404 paths, scope-change update semantics) on sqlite+postgres; conformance case proving an ENDPOINT policy rejects over-budget traffic on that endpoint but not another; recorder case for streaming route enforcement; webhook payload asserted in Rust webhook unit tests; schema diff vs merged `latest_schema.sql` for `budget_policies`.

- [ ] T-S7 Gateway providers: Portkey routing, SAP AI Core, multimodal content + get_span_image
  - **Upstream refs:** `1a11a620e3` Portkey routing configuration (#24398); `315d174b63` sap-ai-core provider + extra_headers (#24356); `8e680541b9` {{ trace }} judges see images via get_span_image (#24590)
  - **Rust target:** `rust/crates/mlflow-server/src/gateway_runtime.rs` (Portkey headers/config split, SAP adapter, Anthropic/Gemini multimodal translation), `gateway_provider_matrix.rs` + provider auth-mode metadata, `rust/crates/mlflow-server/src/gateway.rs` endpoint-config storage split (secret_value vs auth_config), `rust/crates/mlflow-genai/src/judge.rs` + `judge_tools.json` + `trace.rs` (get_span_image tool, tool-response ordering protocol), scorer config `extra_headers` passthrough in `rust/crates/mlflow-genai`
  - **AC:** (1) Portkey: config fields `portkey_provider` (auth_config), `portkey_config` (secret), `provider_api_key` (secret), `api_base` default `https://api.portkey.ai/v1`; request headers `x-portkey-api-key` always, `x-portkey-provider`/`x-portkey-config` conditional, `Authorization: Bearer <provider_api_key>`; on passthrough routes a configured provider_api_key **replaces** any client Authorization header (case-insensitive delete), otherwise base passthrough behavior; provider auth-mode metadata matches merged `mlflow/utils/providers.py`. (2) sap-ai-core: new provider, empty config, base URL from `MLFLOW_GENAI_JUDGE_BASE_URL` validated at request time (unset → INVALID_PARAMETER_VALUE; scheme http/https; trailing slash stripped), no auth headers, Orchestration-v2 request/response translation with upstream's param whitelist, chat-only (completions/embeddings unsupported). (3) OpenAI-style list content translates on Anthropic (base64 data-URL → image block, non-base64 → `[unsupported image reference: <url>]` text) and Gemini (`inlineData`) endpoints; `parse_base64_data_url` semantics match. (4) If Rust judge jobs own the tool loop: `get_span_image` tool registered with upstream's attachment-ref scanning, default-first-image selection, 10 MiB cap (`MLFLOW_TRACE_MAX_ATTACHMENT_SIZE` override >0), error-string (not exception) failure modes, and the tool-response-block-then-image-user-turn ordering protocol with image turns dropped alongside their pair on truncation; scorer configs accept and forward `extra_headers` (persisted wire-format field).
  - **VER:** Conformance rows for Portkey endpoint create (secret vs auth_config placement asserted via store round-trip) and header synthesis against a stub upstream; sap-ai-core translation unit tests + conformance error cases (missing env, bad scheme); recorder differential for Anthropic/Gemini multimodal passthrough; Rust judge unit tests for get_span_image tool registration/caps/protocol (or documented N/A if the tool loop is Python-side); scorer `extra_headers` round-trip corpus case.

- [ ] T-S8 Assistant runtime parity (streams, providers, LLM-connection keys)
  - **Upstream refs:** `555774ccc3` Drop stream_options (#24552); `98162559e0` Session token usage `cache_read_tokens` (#24507); `10f819bbea` Truncated streams surface as errors (#24603); `621bf084c4` API keys in LLM Connections (#24581); `57f8f07e4d` Auto-choose provider (#24584); `c6ad4d51a0` Popular-provider quick select, `GET /providers` (#24582)
  - **Rust target:** `rust/crates/mlflow-server/src/assistant.rs` + `assistant_providers/` + `openai_compatible.rs`
  - **AC:** (1) Outbound chat-completions requests no longer send `stream_options`. (2) Usage SSE payloads include `cache_read_tokens` (subset of prompt_tokens; Claude `cache_read_input_tokens or 0`). (3) OpenAI-compatible stream loop tracks productive signal ([DONE], usage, finish_reason, content, tool_calls, error frame); mid-stream error frames → SSE `error` `{"error": "<DisplayName> error: <message>"}` and terminate without `done`; fully silent streams → the exact upstream empty-response error text. (4) `PUT /config` stores API keys as gateway secret/model-definition/endpoint triples named `mlflow-assistant-<vendor>` with upstream's validation 400s (vendor without key, key on non-gateway provider, key without vendor, unsupported-backend message) and overwrites the provider model with the endpoint name; keys never persist in assistant config. (5) Provider resolution: explicit selection wins; else claude_code → codex → mlflow_gateway filtered by remote-access; `GET /config` synthesizes the resolved provider (gateway excluded) with `selected: true`; stream route emits SSE error "No assistant provider is configured or available." when none; `POST /message` returns the corrected `/sessions/{id}/stream` URL; skills install still requires explicit selection. (6) New `GET /providers` (localhost-only) with upstream's response shape (`providers[]`, `resolved` incl. `auto_selected`/vendor inference from the `mlflow-assistant-` endpoint-name prefix, `gateway_vendor_options`); Codex display name "Codex".
  - **VER:** `uv run --no-sync pytest -q rust/compliance/recorders/` extended with: truncated-stream error case, mid-stream error frame case, cache_read_tokens usage frame, no-provider SSE error; conformance rows for `GET /providers`, `PUT /config` key-storage validation matrix and gateway-triple creation, provider auto-selection (localhost vs remote); byte-level SSE frame checks preserved.

- [ ] T-S9 Misc server parity: compact trace JSON, model catalogs, saved-view envelope
  - **Upstream refs:** `60a270b8e1` Optimize protobuf JSON serialization (#24802); `bdd0c77a1a`+`ef7d58602f`+`4bed86bf24`+`b1e5ef1d91`+`8f7aadcb2b` model catalog updates; `39433e6dd5` Named saved views envelope (#24360)
  - **Rust target:** `rust/crates/mlflow-server/src/proto_http.rs` / trace handler response serialization; `mlflow-server/build.rs` embedded catalogs + `rust/genai-inventory/` ledger; experiment-tag limits check
  - **AC:** (1) The six trace endpoints (StartTraceV3, GetTraceInfoV3, BatchGetTraceInfos, SearchTracesV3, BatchGetTraces, GetTrace) emit compact JSON exactly like merged Python (no indent; int64-as-number convention unchanged elsewhere; TraceInfo endpoints' conversion flag is value-neutral today) so byte-level differentials stay green. (2) Rust-served model catalog data equals the merged five-commit end state (note `b1e5ef1d91` removes azure_ai entries — removals must propagate). (3) Saved views need no new endpoints: verify experiment tag CRUD accepts `mlflow.sharedViewState.<id>` keys with compressed-envelope-sized values identically to Python (length limits) and GetExperiment returns them — coverage, not code, unless a divergence is found.
  - **VER:** Corpus replay over the six trace endpoints (byte comparison catches formatting drift both ways); `rust/genai-inventory/validate_ledger.py` green + catalog conformance rows incl. an azure_ai removed-model case; corpus case setting/getting a realistic saved-view tag envelope at the size boundary.

- [ ] T-S10 Rebuild the upstream UI and run UI smoke
  - **Upstream refs:** all 30 `ui` commits (MCP registry UI, saved views, Assistant panel/settings/providers, issue detection background jobs, presigned artifact downloads in UI, permission-denied notifications)
  - **Rust target:** production React static build served by the Rust deployment
  - **AC:** The merged UI builds cleanly and all e2e-covered surfaces work against Rust with zero `X-MLflow-Backend: python` responses and no unexpected 4xx; new UI surfaces that call new server routes (MCP registry detail/access endpoints, Assistant `GET /providers`, presigned downloads, saved views) function against the Rust implementations from T-S1/T-S2/T-S8.
  - **VER:** `bash rust/e2e/run.sh` — all phases green (production build rebuilt in Phase 1); extend Playwright coverage for the MCP registry detail page and Assistant provider quick-select if not already covered.

## Skipped

- `client-sdk` (11 commits: tracing contextvars/otel propagation, langgraph, notebook display, pydantic-ai autologging, kubernetes auth, pandas 3.x, ml-package-versions, env_pack ARM, docstrings): the Python client remains the supported client; never ported.
- `server-api`-bucketed but client-only after inspection: `2f65592d3e` (Windows artifact download paths — client download destination sanitization, no wire change); `3d5041e38a` + `7e29ca6c54` (Unity Catalog native temp-creds/2.1 gating — MLflow-as-client talking to Databricks UC, no tracking-server surface); `f7d2e5a372` (mlflow.genai.evaluate dataframe prep — SDK-side); `8620608eb8` (version-parse hardening — Python runtime robustness + clint lint rule, no HTTP/DB behavior).
- `infra` (53 commits: CI workflows incl. `$/` action syntax and xdist parallelization, flaky-test pipeline, uv.lock, docs, typescript/claude-code integration packaging, Helm cronjob imagePullSecrets, release-3.15.0 version bumps): merged as maintenance; no Rust behavior to port.

## Completion checklist

- [ ] Unary differential corpus replay is green:
      `uv run --no-sync python rust/compliance/replay.py`.
- [ ] Required Python-over-HTTP conformance matrix is green:
      `uv run --no-sync python rust/genai-inventory/run_conformance.py --profile required`.
- [ ] SSE/streaming recorder differentials are green:
      `uv run --no-sync pytest -q rust/compliance/recorders/`.
- [ ] Three-phase Playwright UI smoke is green: `bash rust/e2e/run.sh`.
- [ ] Production UI was rebuilt if the `ui` bucket was non-empty.
- [ ] New upstream endpoints have new corpus/conformance cases, not code-only coverage.
- [ ] `rust/sync/state.json` advances to `0489fb31d8a144ec3c80d15eb44dbe24b790465e` and records this plan.
